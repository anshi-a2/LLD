spring:
  application:
    name: login
  datasource:
    url: jdbc:h2:mem:logindb
    driverClassName: org.h2.Driver
    username: anshiagg
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 8081
